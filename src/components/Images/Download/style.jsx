import styled from 'styled-components';

export const DownloadContainer = styled('svg')(()=>({
    height: '24px',
    width: '24px',
    margin: 'auto',
    fill: {
        color: 'rgb(64, 159, 255)'
    }
}));